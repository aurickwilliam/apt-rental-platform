"use client";

import { Button } from "@heroui/react";

export default function DiscoverNowBtn() {
  return (
    <Button
      variant="bordered"
      radius="full"
    >
      Discover Now
    </Button>
  );
}